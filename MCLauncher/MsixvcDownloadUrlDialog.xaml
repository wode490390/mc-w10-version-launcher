<Window x:Class="MCLauncher.MsixvcDownloadUrlDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select MSIXVC download link" Height="380" Width="720" MinHeight="300" MinWidth="560"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Multiple MSIXVC download links are available. Select the link to use:" TextWrapping="Wrap" />
        <TextBlock Grid.Row="1" Name="VersionLabel" Margin="0,4,0,8" FontWeight="Bold" TextWrapping="Wrap" />
        <ListBox Grid.Row="2" Name="UrlsListBox" DisplayMemberPath="DisplayName"
                 SelectionChanged="UrlsListBox_SelectionChanged"
                 MouseDoubleClick="UrlsListBox_MouseDoubleClick" />
        <TextBox Grid.Row="3" Name="SelectedUrlTextBox" Margin="0,8,0,12" Height="60"
                 IsReadOnly="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" />
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Name="OkButton" Content="OK" Width="80" Margin="0,0,8,0" IsDefault="True" Click="okButton_Click" />
            <Button Content="Cancel" Width="80" IsCancel="True" Click="cancelButton_Click" />
        </StackPanel>
    </Grid>
</Window>
